@using FInalProject.ViewModels;
@model BookFocusViewModel;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewBag.Title = "Book Focus";
}
<center>
    <h2>Book</h2>
    <hr />
    <div class="card">
        <h3>@Model.BookName</h3>
        <h3>@Model.Description</h3>
        <h3>@Model.BookAuthorName</h3>
        <h3>@Model.BookCover</h3>
        <h3>@Model.BookReadingTime</h3>
        <h3>@Model.BookPages</h3>
        <h3>@Model.DateTaken</h3>
        <h3>@Model.UntillReturn</h3>
        @foreach(var genre in Model.genres)
        {
            <h4>@genre.Name</h4>
        }
        @if (User.IsInRole("Librarian"))
        {
            //<a href="@Url.Action("DeleteBook", "BookOperations", new {doomedId = Model.BookId})" class="btn-primary">Delete</a>
            <a href="javascript:void(0)" onclick="DeleteBook(@Model.BookId)" class="btn-primary">Delete</a>
           
        }
        <h3>Comment Section</h3>
        <hr />
        @{
            await Html.RenderPartialAsync("_CreateComment", new FInalProject.ViewModels.CreateCommentViewModel
            {
                BookId = Model.BookId
            });
        }
        <hr />

        @if(Model.comments == null)
        {
            <h2>No comments yet</h2>
        }
        else
        {
            @foreach(var comment in Model.comments)
            {
                <label for="UserName">User name:</label>
                <h4 class="UserName">@comment.UserName</h4>
                <h4 class="Description">@comment.Description</h4>
            }
        }
    </div>
</center>
@section Scripts {
    <script src="~/js/site.js"></script>
}